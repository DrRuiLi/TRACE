% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TRACE_global_assignment.R
\name{TRACE_score_mat_component}
\alias{TRACE_score_mat_component}
\title{Recompute path-compatibility score matrix for one component}
\usage{
TRACE_score_mat_component(
  object,
  i.pol = 0L,
  conn.component,
  max_path_length = 5L,
  connection_cutoff = 0.5
)
}
\arguments{
\item{object}{MSdev object with TRACE network assignment results.}

\item{i.pol}{Polarity index (`0` = negative, `1` = positive).}

\item{conn.component}{Numeric connectivity component id (e.g. `638`).}

\item{max_path_length}{Maximum simple path length used when scoring (default `5`).}

\item{connection_cutoff}{Compatibility cutoff used to build the adjacency matrix (default `0.5`).}
}
\value{
A list with `nodes`, `assign` (subset of `cn.seed.assign`),
  `score_mat`, `blocked_mat`, `ppm.dyn`, `rt.tol.dyn`, `connection_cutoff`,
  and `max_path_length`.
}
\description{
This is a debugging/inspection helper that reproduces the internal
`score_mat` and `blocked_mat` used to derive `assign.group` within a
connectivity component (`conn.component`).
}
