% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.pave.R
\name{map_pave_seed_vertex_group}
\alias{map_pave_seed_vertex_group}
\title{Map PAVE \code{pave_seed} (m/z) to vertex group labels for network plots}
\usage{
map_pave_seed_vertex_group(
  feature_id,
  pave_seed,
  pave.trace.merged,
  type_pave = NULL,
  ppm = 10,
  mz_col = "mz_pave",
  id_col = "feature_id"
)
}
\arguments{
\item{feature_id}{Feature IDs for nodes in the demo subgraph.}

\item{pave_seed}{PAVE seed m/z per node (may be \code{NA}).}

\item{pave.trace.merged}{Merged PAVE/TRACE table used for m/z lookup.}

\item{type_pave}{PAVE annotation type per node (used to detect metabolites).}

\item{ppm}{Maximum ppm error when matching \code{pave_seed} to a feature m/z.}

\item{mz_col}{Column in \code{pave.trace.merged} with m/z values.}

\item{id_col}{Column in \code{pave.trace.merged} with feature IDs.}
}
\value{
Named character vector of group labels keyed by \code{feature_id}.
  Value \code{"noise"} means no PAVE seed.
}
\description{
In PAVE exports, \code{pave_seed} is the metabolite seed m/z, not a feature
ID. Each \code{pave_seed} is matched to the closest m/z in
\code{pave.trace.merged} (within \code{ppm}) to obtain a seed
\code{feature_id} group label. PAVE metabolites without \code{pave_seed} are
their own seed (\code{feature_id}). Other nodes without \code{pave_seed} are
labeled \code{"noise"} (plot as grey). When no merged feature is within
\code{ppm}, features cluster by the seed m/z string.
}
