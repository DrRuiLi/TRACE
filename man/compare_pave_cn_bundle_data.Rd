% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.pave.R
\name{compare_pave_cn_bundle_data}
\alias{compare_pave_cn_bundle_data}
\title{Build PAVE vs TRACE CN bundle comparison data}
\usage{
compare_pave_cn_bundle_data(
  pave.trace.merged,
  object,
  i.pol = 0,
  mz_error = c("max", "mean")
)
}
\arguments{
\item{pave.trace.merged}{Data frame from a PAVE/TRACE feature merge.}

\item{object}{MSdev object with TRACE results in `object@advancedAna$TRACE_temp`.}

\item{i.pol}{Polarity index. `0` for negative and `1` for positive.}

\item{mz_error}{Character. Use `"max"` or `"mean"` absolute m/z error (ppm)
across bundle edges.}
}
\value{
A data frame with one row per CN seed (`feature_id`), comparison
  group (`cn_cmp`), and bundle metrics `cn_bundle_cor` / `cn_bundle_mz_ppm`.
}
\description{
Classify CN seeds as `unmatched_blank` (PAVE non-blank, TRACE blank) or
`matched_cn` (non-blank in both), then extract each seed's CN bundle from the
MSdev object. Bundle correlation and m/z error are calculated from the CN
pattern edges in each bundle.
}
